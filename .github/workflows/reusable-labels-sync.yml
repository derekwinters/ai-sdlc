name: labels-sync

# The label taxonomy is the pipeline's state machine, so it lives in git and is
# applied from there. Runs when a manifest changes, and on demand.

on:
  workflow_call:
    inputs:
      ref:
        description: >-
          The ai-sdlc ref providing the sync. Leave unset when ai-sdlc is
          itself the caller.
        type: string
        default: ""
      directory:
        description: Where the two manifests live in the calling repository.
        type: string
        default: .github

permissions:
  contents: read
  issues: write

concurrency:
  group: labels-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Fetch the sync
        if: inputs.ref != ''
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          repository: derekwinters/ai-sdlc
          ref: ${{ inputs.ref }}
          # Not `.ai-sdlc`: that is where the *consumer* keeps its committed
          # configuration, and checkout empties the directory it writes into.
          path: .ai-sdlc-checkout

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"

      - name: Apply the taxonomy
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          SKILL_ROOT: ${{ inputs.ref != '' && '.ai-sdlc-checkout' || '.' }}
        run: |
          PYTHONPATH="$SKILL_ROOT:$SKILL_ROOT/skills/labels/label-sync" \
            python3 "$SKILL_ROOT/skills/labels/label-sync/label_sync.py" \
            "${{ inputs.directory }}"
